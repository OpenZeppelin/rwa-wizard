---
'@openzeppelin/codegen-core': minor
---

Add `omitExactConfigPath` for emit-path attribution hygiene: drop a config path
from a recorded path list by exact string equality before the list reaches an
emission. Documents hazard 5 (list-scan vs whole-list dependence) in
`docs/codegen-core/provenance/attribution-hazards.md`. Matching rules,
builders, and generated bytes are unchanged. Stellar role-guard adoption ships in
`quieter-addresses-role-guard.md`.
