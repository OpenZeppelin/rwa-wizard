/**
 * Default role symbol mapping for well-known RWA role names.
 * These are conventional names used across RWA systems regardless of chain.
 */
export const DEFAULT_ROLE_SYMBOLS: Record<string, string> = {
  manager: 'manager',
  agent: 'agent',
  operator: 'operator',
};
