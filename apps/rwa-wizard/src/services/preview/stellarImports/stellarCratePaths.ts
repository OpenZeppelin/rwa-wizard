/** Keys are Rust identifier prefixes as they appear in `use` lines. */
export const STELLAR_CRATE_REPO_PATHS: Readonly<Record<string, string>> = {
  stellar_access: 'packages/access',
  stellar_tokens: 'packages/tokens',
  stellar_macros: 'packages/macros',
  stellar_contract_utils: 'packages/contract-utils',
};
